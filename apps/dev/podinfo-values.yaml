apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  chart:
    spec:
      version: "6.4.0"
  values:
    ingress:
      hosts:
        - host: podinfo.markguiang.dev
          paths:
            - path: /
              pathType: ImplementationSpecific
